# Neovim Configuration

## Location
This folder contains the configuration files for Neovim.

Run the following command to create a symbolic link to the configuration files:
```bash
ln -s $PWD/nvim $HOME/.config/nvim
```
